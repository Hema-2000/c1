#include <stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    FILE *fp=NULL;
    char str[50];
    char ch='s';
    fp=fopen("abc.txt","r");
    if(fp==NULL)
    {
        printf("error");
        exit(1);
    }
    /*while(!feof(fp))//loop to read entire string[feof-end of the file--> predefined function]
    {
    ch=fgets(fp);
    printf("%c",ch);
    }*/
    while(!feof(fp))//condition for to read new line statement also
    {
    fgets(str,20,fp);
    printf("%s",str);
    }
    fclose(fp);
}
